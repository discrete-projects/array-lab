'use strict'

let arrayNum = [1, 2, 3]
let arrayChar = ['a', 'b', 'c']

const concatFunc = (arr1, arr2) => {
  let newArr = []
  return newArr = arr1.concat(arr2)
}

console.log(concatFunc(arrayNum, arrayChar))
